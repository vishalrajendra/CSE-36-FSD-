var age=20;
        const x=30;
        {
            let y=10;
            document.write("let" + y + "<bar>");
        }
        document.write("age" + (++age) + "<br>");
        // document.write("let y" + y);